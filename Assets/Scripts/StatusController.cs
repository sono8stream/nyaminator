using UnityEngine;
using System.Collections;

public class StatusController : MonoBehaviour
{
    public int power, civilization, cooperation;

    // Use this for initialization
    void Start()
    {
        power = 30;
        civilization = 30;
        cooperation = 30;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
